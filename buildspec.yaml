version: 0.2

phases:
  pre_build:
    commands:
      - echo "Pre-build phase"

  build:
    commands:
      - echo "Build phase completed"

artifacts:
  files:
    - buildspec-deploy.yaml
    - deploy-k8s.sh
    - k8s//*
  discard-paths: no
